import { FullnameTrimDirective } from './fullname-trim.directive';

describe('FullnameTrimDirective', () => {
  it('should create an instance', () => {
    const directive = new FullnameTrimDirective();
    expect(directive).toBeTruthy();
  });
});
